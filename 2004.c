#include<stdio.h>
int main()
{
    char a[1000];
    int A=0,B=0,i,j,n;
    scanf("%s",a);
    n=strlen(a);
    for(i=0;i<n;i++)
    {
        if(a[i]=='A'&&a[])
        A++;
        else
        B++;
    }
}
