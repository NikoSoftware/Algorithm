#include<stdio.h>
int mai()
{
    int n,a,b,k,a[100],c=0;
    scanf("%d",&n);
    while(n--)
    {
        scanf("%d%d",&a,&b);
        for(int i=0;i<b;i++);
        {
            scanf("%d",&k);
            a[i]=k;
        }
        printf("Case #%d",);
    }
    return 0;
}
